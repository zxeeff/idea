# Every peer uses Codex Daybreak Blue at max reasoning effort. Adaptive mode
# grows exclusively from this template; fixed mode creates exactly 16 peers.

[[agents]]
provider = "openai"
model = "gpt-daybreak-blue-latest"
effort = "max"
count = 16
