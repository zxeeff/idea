# Every peer uses Codex Daybreak Blue at max reasoning effort.
# This preset creates exactly 16 fixed peers.

[[agents]]
provider = "openai"
model = "gpt-daybreak-blue-latest"
effort = "max"
count = 16
